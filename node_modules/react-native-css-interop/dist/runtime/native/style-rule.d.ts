import { ContainerRecord, StyleRuleSet } from "../../types";
import { Effect } from "../observable";
import { VariableContextValue } from "./styles";
export type Refs = {
    props: Record<string, any>;
    variables: VariableContextValue;
    containers: ContainerRecord;
};
export declare function createStyleRuleSetObservable(ruleSet: StyleRuleSet): {
    get: (effect: Effect, callback: () => void) => void;
};
